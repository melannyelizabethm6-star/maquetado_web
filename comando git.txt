git init 
(sirve para iniciar el repositorio inicial)

git config --local user.name "Melanny"
(configura el nombre dde usuario manualmente)

git config --local user.email "melannyelizabethm6@gmail.com"
(configura el email asociado a la cuenta de github)

git branch -M main
(cambiar el nombre de la rama de Master a Main)

git add .
(añade todos los archivos al repositorio)

git commit -m "version de subida"
(es la version que esta subiendo al repo)

git push -u origin main
(este emouja los cambios locales a la nube)
git 
git status 
(comprueba el estado de la rama)

git remote add origin 
https://github.com/melannyelizabethm6-star/maquetado_web.git
(añade la direccion de su repositorio se hace o se ejecuta una unica vez)

xd
cambio 

